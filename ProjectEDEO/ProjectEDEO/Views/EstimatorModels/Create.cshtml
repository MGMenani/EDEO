@model Project_EDEO.Models.EstimatorModel

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    @using (Html.BeginForm(new { @enctype = "multipart/form-data", @class = "form-horizontal" }))
    {
        @Html.AntiForgeryToken()
        <div class="card-header">
            <strong>Create new</strong> estimator model
        </div>
        <div class="card-body card-block">
            <div class="row form-group">
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="col col-md-3"><label for="text-input" class=" form-control-label">Name</label></div>
                <div class="col-12 col-md-9">
                    @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @id = "text-input", @name = "text-input", @placeholder = "Name", @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Name, "", new { @class = "form-text text-muted text-danger" })
                </div>
            </div>
            <div class="row form-group">
                <div class="col col-md-3"><label for="text-input" class=" form-control-label">Main file</label></div>
                <div class="col-12 col-md-9">
                    @Html.EditorFor(model => model.PythonFile, new { htmlAttributes = new { @id = "text-input", @name = "text-input", @placeholder = "Python file", @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.PythonFile, "", new { @class = "form-text text-muted text-danger" })
                </div>
            </div>
        </div>
        <div class="card-footer">
            <button type="submit" class="btn btn-primary btn-sm">
                <i class="fa fa-dot-circle-o"></i> Next
            </button>
            <button type="reset" class="btn btn-danger btn-sm">
                <i class="fa fa-ban"></i> Reset
            </button>
        </div>
    }
</div>

<div>
    <a href=@Url.Action("Index", "MedicalRecords") class="btn btn-outline-secondary" type="button">Back to list</a>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}